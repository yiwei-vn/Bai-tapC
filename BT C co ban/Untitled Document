#include<stdio.h>

 
int main(){
    int  a, b;
    a=20;
    b=21;
if( a%2 == 0 ){
    printf("%d la so chan", a );
    }
else{
    printf("%d la so le", a );
    }
if ( a%2 != 0 ){
    printf("%d la so chan", b );
    }
else{
    printf("%d la so le", b );
    }

return 0;
}
